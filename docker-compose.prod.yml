version: "3.8"
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        REACT_APP_API_URL: http://localhost:4000   # o la URL real del backend en prod
    container_name: umg_frontend_prod
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production